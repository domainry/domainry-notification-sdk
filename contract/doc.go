// Package contract contains stable Notification wire values. It intentionally
// excludes server-derived workspace, viewer, reporting-line and delegation
// scopes; implementations derive those values after Identity authorization.
package contract
